# 🛒 WB Deal Finder (Парсер цен Wildberries)

Инструмент для мониторинга цен конкретных продавцов на Wildberries.
Скрипт автоматически обходит защиту от ботов, сохраняет историю цен в базу данных (SQLite) и выявляет реальные скидки на основе медианной цены.

## 🚀 Возможности
- **Автоматический сбор:** Парсит весь каталог продавца (все страницы).
- **Умная авторизация:** Использует реальный браузер (Edge/Chrome) для получения токенов.
- **Аналитика:** Считает *честную* медиану цены (а не ту, что рисует WB) и показывает реальный процент скидки.
- **История:** Все цены сохраняются в `wb_prices.db` для отслеживания динамики.
- **Экспорт:** Готовый отчет в Excel с подсветкой выгодных предложений.

## 🛠 Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone [https://github.com/ВАШ_НИК/wb-deal-finder.git](https://github.com/ВАШ_НИК/wb-deal-finder.git)
   cd wb-deal-finder

## ▶️ Использование
1. Первый запуск (Авторизация)
Запустите скрипт:

Bash

python parser.py

## 2. Как поменять продавца?
Откройте файл parser.py в любом редакторе кода. Прокрутите в самый низ до блока if __name__ == "__main__":. Найдите список sellers_list:

Python

if __name__ == "__main__":
    # ...
    # ЗАМЕНИТЕ ID В ЭТОМ СПИСКЕ:
    sellers_list = [
        515469,  # ID продавца 1 (например, Тату-магазин)
        123456,  # ID продавца 2 (можно добавить через запятую)
    ]
    # ...

## 📂 Структура проекта
parser.py — Основной код парсера и запуска браузера.

wb_browser_profile/ — Папка с вашей сессией (НЕ УДАЛЯТЬ, пока работает).


Disclaimer: Этот инструмент создан исключительно в образовательных целях для демонстрации работы с Playwright и SQLite. Автор не несет ответственности за использование скрипта.