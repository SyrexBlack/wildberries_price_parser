# 🛒 WB Deal Finder (Парсер цен Wildberries)

**WB Deal Finder** — это инструмент для мониторинга цен и ассортимента конкретных продавцов на Wildberries.

Скрипт автоматически обходит защиту от ботов (Playwright), сохраняет историю цен в локальную базу данных (SQLite) и выявляет реальные скидки на основе медианной цены, игнорируя маркетинговые "накрутки" маркетплейса.

---

## 🚀 Возможности

- **🤖 Автоматический сбор:** Парсит весь каталог выбранного продавца, проходя по всем страницам пагинации.
- **🧠 Умная авторизация:** Использует реальный браузер (Edge/Chrome) для перехвата токенов авторизации, что позволяет работать через внутреннее API WB.
- **📊 Честная аналитика:** Считает медианную цену товара за период, а не ту, что отображается на сайте, и показывает реальный процент скидки.
- **💾 История цен:** Все данные сохраняются в `wb_prices.db` (SQLite) для отслеживания динамики изменений.
- **📑 Экспорт:** Генерирует готовый отчет в Excel (`.xlsx`) с подсветкой самых выгодных предложений.

---

## 🛠 Установка

Для работы требуется установленный **Python 3.8+**.

1. **Клонируйте репозиторий:**
   ```bash
   git clone [https://github.com/ВАШ_НИК/wb-deal-finder.git](https://github.com/ВАШ_НИК/wb-deal-finder.git)
   cd wb-deal-finder
## Установите зависимости:
```
pip install -r requirements.txt
```

## Установите браузеры для Playwright:
```
playwright install msedge
```
(Если у вас нет Edge, скрипт можно перенастроить на Chrome)

## ▶️ Использование
**1. Первый запуск (Авторизация)**
При первом запуске скрипту необходимо создать профиль пользователя.

Запустите парсер:
```
python parser.py
```

- Откроется окно браузера с сайтом Wildberries.

- Нажмите кнопку "Войти".

- Введите номер телефона и код из СМС.

- Как только вход будет выполнен, скрипт автоматически перехватит сессию, закроет браузер и начнет сбор данных.

## ℹ️ Важно: Сессия сохраняется в папку wb_browser_profile. При следующих запусках повторная авторизация не требуется.

**2. Настройка списка продавцов**
Чтобы изменить список магазинов для мониторинга:
```
Откройте файл parser.py в любом редакторе кода.

Прокрутите в самый низ до блока if __name__ == "__main__":.

Найдите список sellers_list и замените ID на нужные вам:

Python

if __name__ == "__main__":
    # ...
    # ЗАМЕНИТЕ ID В ЭТОМ СПИСКЕ:
    sellers_list = [
        515469,  # ID продавца 1 (например, Тату-магазин)
        123456,  # ID продавца 2 (можно добавить через запятую)
    ]
    # ...
ID продавца можно взять из URL ссылки на его магазин (например: wildberries.ru/seller/515469).
```
**3. Обновление токена**
```
Если скрипт перестал собирать данные (ошибка 401 или пустой список товаров):

Удалите папку wb_browser_profile.

Запустите скрипт заново — он снова откроет браузер для переавторизации.
```   
## 📂 Структура проекта
parser.py — Основной исполняемый файл. Содержит логику запуска браузера, перехвата токенов и парсинга API.

wb_browser_profile/ — Папка с профилем браузера и cookies (ваша сессия). Не удаляйте её, пока авторизация актуальна.

requirements.txt — Список необходимых библиотек.

## ⚠️ Disclaimer
Этот инструмент создан исключительно в образовательных целях для демонстрации возможностей библиотек Playwright, curl_cffi и sqlite3. Автор не несет ответственности за использование скрипта. Используйте на свой страх и риск.
